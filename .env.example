# Whop SDK Environment Variables
# Get these from your Whop Developer Dashboard: https://whop.com/dashboard/developer

# Your app's API key (keep secret, server-side only)
WHOP_API_KEY=get_this_from_whop_dashboard

# Webhook secret for verifying webhook signatures (optional, if using webhooks)
WHOP_WEBHOOK_SECRET=get_this_after_creating_webhook

# Your app's ID (public, can be exposed to client)
NEXT_PUBLIC_WHOP_APP_ID=your_app_id
